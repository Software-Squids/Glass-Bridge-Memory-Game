import { createTheme } from "@mui/material";


let defaultTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#DF245C'
    },
    secondary: {
      main: '#FFFFFF'
    },
  }
});

export default defaultTheme;